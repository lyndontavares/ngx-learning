import { EmailInvoiceDirective } from './email-invoice.directive';

describe('EmailInvoiceDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailInvoiceDirective();
    expect(directive).toBeTruthy();
  });
});
