import { DuplicateInvoiceDirective } from './duplicate-invoice.directive';

describe('DuplicateInvoiceDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateInvoiceDirective();
    expect(directive).toBeTruthy();
  });
});
