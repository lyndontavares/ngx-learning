import { ParseIntDirective } from './parse-int.directive';

describe('ParseIntDirective', () => {
  it('should create an instance', () => {
    const directive = new ParseIntDirective();
    expect(directive).toBeTruthy();
  });
});
