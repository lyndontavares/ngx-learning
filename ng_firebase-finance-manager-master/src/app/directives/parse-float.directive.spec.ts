import { ParseFloatDirective } from './parse-float.directive';

describe('ParseFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new ParseFloatDirective();
    expect(directive).toBeTruthy();
  });
});
